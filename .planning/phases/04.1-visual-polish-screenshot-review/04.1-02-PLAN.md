---
phase: 04.1-visual-polish-screenshot-review
plan: 02
type: execute
wave: 2
depends_on: ["04.1-01"]
files_modified:
  - CLAUDE.md
autonomous: true
requirements: [DSGN-01, DSGN-03, BROW-05, BROW-06]

must_haves:
  truths:
    - "CLAUDE.md Visual Review Protocol is mandatory (MUST, not SHOULD) for any plan touching UI"
    - "The protocol includes the complete build-pagefind-screenshot-review-fix cycle"
    - "The protocol lists all current screenshots including any added in Plan 01"
    - "Future Claude sessions following CLAUDE.md will autonomously validate visual output without user intervention"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Strengthened Visual Review Protocol with mandatory language"
      contains: "MUST"
  key_links:
    - from: "CLAUDE.md"
      to: "e2e/screenshots/"
      via: "Screenshot file list in Visual Review Protocol"
      pattern: "screenshots/"
---

<objective>
Strengthen CLAUDE.md Visual Review Protocol from advisory to mandatory, ensuring future phases that touch UI automatically run the build-screenshot-review-fix cycle and block commits until visual quality passes. Update screenshot lists to reflect any new screenshots added in Plan 01.

Purpose: After this, Claude + GSD autonomously validates visual output for every future UI change -- no user hand-holding needed.
Output: Updated CLAUDE.md with mandatory visual review protocol.
</objective>

<execution_context>
@/opt/claude-config/get-shit-done/workflows/execute-plan.md
@/opt/claude-config/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-visual-polish-screenshot-review/04.1-CONTEXT.md
@.planning/phases/04.1-visual-polish-screenshot-review/04.1-01-SUMMARY.md

@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strengthen CLAUDE.md Visual Review Protocol</name>
  <files>CLAUDE.md</files>
  <action>
Update the existing Visual Review Protocol section in `CLAUDE.md` to make it mandatory and comprehensive:

1. **Strengthen language from advisory to mandatory:**
   - Change "you MUST" language is already present but ensure it covers the COMPLETE cycle
   - Make clear: after ANY plan execution that modifies HTML templates (`scripts/generate-pages.js`), CSS (`src/css/main.css`), JS (`src/js/main.js`), or assets (`src/assets/`), the Visual Review Protocol MUST be followed
   - Add explicit: "Do NOT commit until all screenshots pass visual review"

2. **Update the screenshot list** to include any new screenshots added during Plan 01 (check `e2e/screenshots/` for the current list). Group by category:
   - Desktop layout screenshots
   - Search screenshots
   - Interactive state screenshots
   - Mobile screenshots
   - Accessibility screenshots
   - Typography screenshots

3. **Strengthen the checklist** to include all validation dimensions from the CONTEXT.md decisions:
   - Layout: no overlapping elements, proper spacing, z-index correct
   - Branding: correct Austrian Gruene arrow-G logo, green colors match CI
   - Typography: proper contrast (WCAG AA), line-height 1.6+, max-width ~70ch
   - Mobile: no horizontal overflow, touch targets adequate
   - Interactive: tooltips, highlights, dropdowns positioned correctly
   - Search UI: dropdown layered, results readable, filter chips aligned
   - Text rendering: umlauts display correctly

4. **Keep the protocol concise and actionable** -- it should be a clear checklist that Claude follows mechanically, not a lengthy policy document.

5. **Do NOT modify shared GSD templates** in `/opt/claude-config/` -- all changes are project-scoped to CLAUDE.md only.
  </action>
  <verify>
    <automated>grep -c "MUST" CLAUDE.md</automated>
  </verify>
  <done>
- CLAUDE.md Visual Review Protocol uses mandatory language ("MUST", "Do NOT commit until")
- Screenshot list is current and complete
- Checklist covers all validation dimensions (layout, branding, typography, mobile, interactive, search, text)
- Protocol is concise and mechanically actionable
  </done>
</task>

</tasks>

<verification>
1. `grep "MUST" CLAUDE.md` shows mandatory language in Visual Review Protocol
2. `grep "screenshots/" CLAUDE.md` shows complete screenshot list
3. Protocol covers all dimensions: layout, branding, typography, mobile, interactive, search
</verification>

<success_criteria>
- CLAUDE.md contains a strengthened, mandatory Visual Review Protocol
- Future Claude sessions reading CLAUDE.md will autonomously run visual validation after UI changes
- No modifications to shared GSD templates in /opt/claude-config/
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-visual-polish-screenshot-review/04.1-02-SUMMARY.md`
</output>
