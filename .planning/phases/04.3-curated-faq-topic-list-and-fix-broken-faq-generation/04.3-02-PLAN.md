---
phase: 04.3-curated-faq-topic-list-and-fix-broken-faq-generation
plan: 02
type: execute
wave: 2
depends_on:
  - 04.3-01
files_modified:
  - data/llm/faq/topics.json
  - data/llm/faq/topics/
autonomous: false
requirements:
  - LLM-02
  - LLM-05

must_haves:
  truths:
    - "npm run llm:faq completes without JSON parse errors"
    - "Generated FAQ topics.json passes npm run llm:validate"
    - "Generated FAQ covers key municipal governance themes with proper references"
    - "Summary generation still works (no regression)"
    - "Glossary generation still works (no regression)"
  artifacts:
    - path: "data/llm/faq/topics.json"
      provides: "Aggregated FAQ content consumed by generate-pages.js"
      contains: "topics"
    - path: "data/llm/faq/topics/"
      provides: "Per-topic generated FAQ JSON files"
  key_links:
    - from: "data/llm/faq/topics.json"
      to: "scripts/generate-pages.js"
      via: "generate-pages reads topics.json to build FAQ HTML"
      pattern: "faq/topics\\.json"
    - from: "data/llm/faq/curated-topics.json"
      to: "data/llm/faq/topics.json"
      via: "generateFAQ reads curated topics and produces topics.json"
      pattern: "curated-topics"
---

<objective>
Run the refactored FAQ generation pipeline, validate output quality, and verify no regressions in summary/glossary pipelines.

Purpose: Produce actual FAQ content using the per-topic architecture from Plan 01, confirm the token limit fix works, and ensure backward compatibility with the page generator.

Output: Generated topics.json + per-topic JSON files, validated and regression-tested.
</objective>

<execution_context>
@/opt/claude-config/get-shit-done/workflows/execute-plan.md
@/opt/claude-config/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.3-curated-faq-topic-list-and-fix-broken-faq-generation/04.3-CONTEXT.md
@.planning/phases/04.3-curated-faq-topic-list-and-fix-broken-faq-generation/04.3-01-SUMMARY.md

@scripts/llm-analyze.js
@scripts/llm-validate.js
@scripts/generate-pages.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run FAQ generation and verify regression safety</name>
  <files>data/llm/faq/topics.json, data/llm/faq/topics/</files>
  <action>
**IMPORTANT:** This task makes actual LLM calls. The `llm-analyze.js` script already unsets the CLAUDECODE env var (line 192) to allow nested Claude CLI calls.

**Step 1: Regression check** -- verify summary and glossary pipelines are unaffected per CONTEXT.md locked decision ("Verify with npm run llm:summaries -- --dry-run and npm run llm:glossary -- --dry-run"):
```bash
npm run llm:summaries -- --dry-run
npm run llm:glossary -- --dry-run
```
Both commands should list their respective items and exit cleanly (no errors). If either errors, STOP and fix Plan 01 output before proceeding.

**Step 2: Run FAQ generation:**
```bash
npm run llm:faq
```
This runs `node scripts/llm-analyze.js --faq --force`. Watch for:
- Each curated topic should log its paragraph match count
- Each topic should produce a callClaude response
- No JSON parse errors
- Final aggregation into topics.json

If any single topic fails, note the slug and retry with `node scripts/llm-analyze.js --faq --topic {slug} --force`.

**Step 3: Validate output:**
```bash
npm run llm:validate
```
Must exit with code 0.

**Step 4: Verify backward compatibility** -- build the site to confirm generate-pages.js can read the new topics.json:
```bash
npm run build
```
The build must succeed. Check that `dist/faq/index.html` and individual topic pages exist.

**Step 5: Run all tests:**
```bash
npm test
```
  </action>
  <verify>
    <automated>npm run llm:summaries -- --dry-run && npm run llm:glossary -- --dry-run && npm run llm:validate && npm test && npm run build</automated>
  </verify>
  <done>FAQ generation completes without errors. topics.json exists with 12+ topics, each with questions and references. llm:validate passes. Site builds successfully with FAQ pages. No regressions in summary or glossary pipelines (both dry-run commands exit cleanly).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of FAQ content quality</name>
  <files>data/llm/faq/topics.json, data/llm/faq/curated-topics.json</files>
  <action>Present the generated FAQ content for human review. The FAQ generation pipeline that was broken due to token limit overflow (34,923 tokens vs 32,000 limit) has been fixed by splitting into per-topic calls.</action>
  <verify>
    1. Check curated topics: `cat data/llm/faq/curated-topics.json | python3 -m json.tool | head -40` -- should show well-structured topics with German titles and seed questions
    2. Check generated FAQ: `cat data/llm/faq/topics.json | python3 -m json.tool | head -60` -- should show topics with questions, answers (gendered language), and cross-BL references
    3. Check per-topic files exist: `ls data/llm/faq/topics/` -- should show 12-18 JSON files
    4. Verify FAQ quality: pick 2-3 topic files and read the answers -- they should describe general principles (not BL-specific claims), use gendered language (Buergermeister:in), and have references to multiple Gemeindeordnungen
    5. Verify site builds: `npm run build && npx pagefind --site dist --force-language de` then check `dist/faq/index.html` exists
    6. Run validation: `npm run llm:validate` should exit 0
  </verify>
  <done>Human approves FAQ content quality -- topics are relevant, answers are well-written with gendered language and proper references, no BL-specific claims in answer text.</done>
</task>

</tasks>

<verification>
- `npm run llm:faq` completes without JSON parse errors
- `npm run llm:validate` exits with code 0
- `npm run build` succeeds (FAQ pages generated)
- `npm test` passes
- `data/llm/faq/topics.json` has 12+ topics with valid structure
- `data/llm/faq/topics/` contains per-topic JSON files
- `npm run llm:summaries -- --dry-run` shows no errors (regression check)
- `npm run llm:glossary -- --dry-run` shows no errors (regression check)
</verification>

<success_criteria>
- FAQ generation pipeline works end-to-end without token overflow
- Generated content has proper gendering, no BL-specific claims, correct citation format
- topics.json is backward-compatible with generate-pages.js
- llm:validate passes
- Summary and glossary pipelines unaffected (both dry-run commands pass)
- Human approves FAQ content quality
</success_criteria>

<output>
After completion, create `.planning/phases/04.3-curated-faq-topic-list-and-fix-broken-faq-generation/04.3-02-SUMMARY.md`
</output>
