---
phase: 06-search-hero-homepage-navigation
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - e2e/tests/hero.spec.js
  - e2e/tests/navigation.spec.js
  - e2e/tests/card-grid.spec.js
  - e2e/tests/mobile.spec.js
  - e2e/tests/search.spec.js
autonomous: false
requirements:
  - SRCH-01
  - SRCH-04
  - SRCH-05
  - NAV-01
  - NAV-02
  - NAV-03

must_haves:
  truths:
    - "E2E tests verify hero search bar is visible and centered on homepage"
    - "E2E tests verify discovery links (FAQ chips, glossary chips if present) are clickable and navigate correctly"
    - "E2E tests verify card grid is inside a collapsible details element"
    - "E2E tests verify header has FAQ and Glossar links on both desktop and mobile viewports"
    - "E2E tests verify hero search produces results in hero dropdown"
    - "E2E tests verify mobile layout has no horizontal overflow and touch-friendly hero search"
    - "All existing E2E tests pass (updated where layout changed)"
    - "Visual Review Protocol screenshots all pass inspection"
  artifacts:
    - path: "e2e/tests/hero.spec.js"
      provides: "Tests for SRCH-01, SRCH-04, SRCH-05: hero layout, discovery links, card grid collapse"
    - path: "e2e/tests/navigation.spec.js"
      provides: "Tests for NAV-01, NAV-02, NAV-03: header consistency, nav links, mobile nav"
    - path: "e2e/tests/card-grid.spec.js"
      provides: "Updated to expect cards inside collapsible details element"
    - path: "e2e/tests/mobile.spec.js"
      provides: "Updated for new index page layout with hero section"
  key_links:
    - from: "e2e/tests/hero.spec.js"
      to: "dist/index.html"
      via: "page.goto('./index.html') with assertions on hero elements"
      pattern: "hero-search-input|discovery-chip|details"
    - from: "e2e/tests/navigation.spec.js"
      to: "dist/index.html and dist/gemeindeordnungen/wien.html"
      via: "page.goto() on multiple pages to verify header consistency"
      pattern: "FAQ|Glossar|nav"
---

<objective>
Create E2E tests for the new hero layout, discovery links, and navigation polish. Update existing tests that break due to the homepage layout change. Run the full Visual Review Protocol.

Purpose: Ensure all Phase 6 requirements are verified by automated tests and visual inspection. Catch regressions in existing functionality.
Output: New hero.spec.js and navigation.spec.js test files, updated card-grid.spec.js and mobile.spec.js, all screenshots passing visual review.
</objective>

<execution_context>
@/opt/claude-config/get-shit-done/workflows/execute-plan.md
@/opt/claude-config/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-search-hero-homepage-navigation/06-CONTEXT.md
@.planning/phases/06-search-hero-homepage-navigation/06-01-SUMMARY.md
@.planning/phases/06-search-hero-homepage-navigation/06-02-SUMMARY.md

@e2e/tests/card-grid.spec.js
@e2e/tests/mobile.spec.js
@e2e/tests/search.spec.js
@e2e/playwright.config.js

<interfaces>
<!-- HTML structure from Plans 01 + 02 (hero section on index page) -->

Expected elements on dist/index.html:
- `#hero-search-input` -- large search input in hero section
- `#hero-search-dropdown` -- results dropdown below hero input
- `#hero-search-chips` -- BL filter chips below hero input
- `.hero-section` -- hero section wrapper
- `.hero-search-container` -- search input container within hero
- `.discovery-section` -- FAQ + Glossar chips section
- `.discovery-chip` -- individual chip links (FAQ topics, glossary terms)
- `details > summary` -- collapsible card grid with "Alle Gesetze durchblattern"
- `details .grid > a` -- card links inside the details element

Expected header elements on ALL pages:
- `nav` with FAQ and Glossar links (no `hidden` class on mobile)
- `#search-input` -- header search input (still present)
- `.gruene-logo` -- logo image
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create hero.spec.js and navigation.spec.js E2E tests</name>
  <files>e2e/tests/hero.spec.js, e2e/tests/navigation.spec.js</files>
  <action>
**Create `e2e/tests/hero.spec.js`** with the following tests:

1. **SRCH-01: Hero search bar visible and centered**
   - Navigate to `./index.html`
   - Assert `#hero-search-input` is visible
   - Assert `.hero-section` exists and contains the search input
   - Assert hero has tagline text (h1 with "Gemeindeordnungen")
   - Screenshot: `e2e/screenshots/hero-search.png`

2. **SRCH-01: Hero search produces results**
   - Navigate to `./index.html`
   - Type "Gemeinderat" into `#hero-search-input`
   - Wait for `#hero-search-dropdown` to become visible (not `.hidden`)
   - Assert dropdown contains at least one result
   - Screenshot: `e2e/screenshots/hero-search-results.png`

3. **SRCH-04: Discovery links visible below hero**
   - Navigate to `./index.html`
   - Assert `.discovery-section` is visible
   - Assert at least 4 `.discovery-chip` elements exist (FAQ topics)
   - Assert first FAQ chip links to `faq/*.html`
   - Click first FAQ chip and verify navigation to FAQ topic page
   - Screenshot: `e2e/screenshots/discovery-links.png`

4. **SRCH-05: Card grid is collapsible**
   - Navigate to `./index.html`
   - Assert `details` element exists with summary containing "Gesetze" text
   - Assert card grid is initially collapsed (details not open)
   - Click the summary to expand
   - Assert Gemeindeordnungen cards (9) are visible inside details
   - Assert Stadtrechte cards (14) are visible inside details
   - Screenshot collapsed: `e2e/screenshots/card-grid-collapsed.png`
   - Screenshot expanded: `e2e/screenshots/card-grid-expanded.png`

**Create `e2e/tests/navigation.spec.js`** with the following tests:

5. **NAV-01: Header consistent across pages**
   - Navigate to `./index.html`, verify header has logo, nav links, search input
   - Navigate to `./gemeindeordnungen/wien.html`, verify same header structure
   - Navigate to `./faq/index.html`, verify same header structure
   - Navigate to `./glossar.html`, verify same header structure
   - Screenshot index header: `e2e/screenshots/header-index.png`
   - Screenshot law page header: `e2e/screenshots/header-law-page.png`

6. **NAV-02: FAQ and Glossar links in header**
   - Navigate to `./index.html`
   - Assert nav contains link with text "FAQ" pointing to `faq/index.html`
   - Assert nav contains link with text "Glossar" pointing to `glossar.html`
   - Click FAQ link, verify navigation to FAQ index page
   - Go back, click Glossar link, verify navigation to glossar page

7. **NAV-03: Mobile navigation**
   - Set viewport to 375x812 (iPhone)
   - Navigate to `./index.html`
   - Assert FAQ and Glossar links are visible (not hidden by `hidden` class)
   - Assert hero search input is visible and full-width
   - Assert no horizontal scrollbar (documentElement.scrollWidth <= viewport width)
   - Assert discovery chips wrap properly (check multiple rows)
   - Screenshot: `e2e/screenshots/mobile-hero.png`
   - Screenshot: `e2e/screenshots/mobile-nav-links.png`

Use the existing test patterns from other spec files (import from `@playwright/test`, use `test.describe` blocks).
  </action>
  <verify>
    <automated>cd /root/workspace && npm run build && npx pagefind --site dist --force-language de && npx playwright test --config=e2e/playwright.config.js e2e/tests/hero.spec.js e2e/tests/navigation.spec.js 2>&1 | tail -30</automated>
  </verify>
  <done>hero.spec.js covers SRCH-01, SRCH-04, SRCH-05 with all tests passing. navigation.spec.js covers NAV-01, NAV-02, NAV-03 with all tests passing. Screenshots captured for hero, discovery links, card grid states, header consistency, and mobile layout.</done>
</task>

<task type="auto">
  <name>Task 2: Update existing E2E tests for new index layout and run full suite</name>
  <files>e2e/tests/card-grid.spec.js, e2e/tests/mobile.spec.js, e2e/tests/search.spec.js</files>
  <action>
**Update `e2e/tests/card-grid.spec.js`:**
- Cards are now inside a `<details>` element that starts collapsed
- Before asserting card visibility, click the `<summary>` to expand the details
- Update selectors: cards are inside `details .grid > a` instead of `section .grid > a`
- Keep the same card count assertions (9 GO + 14 SR)
- Update screenshot to capture expanded state: `e2e/screenshots/card-grid-index.png`

**Update `e2e/tests/mobile.spec.js`:**
- Mobile index page now has hero section as primary element
- Update assertions to check for hero section visibility on mobile
- Check that hero search input is visible and properly sized for touch (min height 44px)
- Check that discovery chips are visible on mobile
- Update screenshot: `e2e/screenshots/mobile-index.png`

**Update `e2e/tests/search.spec.js`** (if needed):
- If tests reference `#search-input` on index page, they may need adjustment since hero search is now primary
- Check if tests navigate to index page and type into `#search-input` -- these should still work since header search exists, but results may appear in hero dropdown instead
- If tests check `#search-dropdown` visibility on index page, update to check `#hero-search-dropdown`
- Ensure tests that run on law pages are unaffected

**Run the FULL test suite** (all specs, all projects) after updates.
  </action>
  <verify>
    <automated>cd /root/workspace && npm run build && npx pagefind --site dist --force-language de && npx playwright test --config=e2e/playwright.config.js 2>&1 | tail -30</automated>
  </verify>
  <done>All existing E2E tests pass with updated selectors. No regressions. Full test suite green across all projects (desktop-chromium, mobile).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of homepage redesign</name>
  <files>e2e/screenshots/</files>
  <action>
Run the Visual Review Protocol per CLAUDE.md. Build, index, capture screenshots, read and inspect all PNGs. Then present to user for human verification.

What was built: Complete search-hero homepage redesign with discovery links, collapsible card grid, polished navigation, and hero search integration. All E2E tests passing.

How to verify:
1. Open the built site: `cd /root/workspace && npx vite preview --port 4173` then visit http://localhost:4173
2. Homepage hero: Verify large search bar is centered and prominent, tagline is clear, green gradient background looks good
3. Hero search: Type "Gemeinderat" in hero search, verify results appear below hero input in expanded panel
4. Discovery links: Verify FAQ topic chips and glossary chips (if present) are visible below hero, click one to verify navigation
5. Card grid: Verify "Alle Gesetze durchblattern" collapsible section, click to expand, verify all 9+14 cards appear
6. Navigation: Verify FAQ and Glossar links in header, check on both desktop and mobile viewport
7. Mobile: Resize browser to ~375px, verify hero search is full-width, discovery chips wrap, no horizontal overflow
8. Law page: Navigate to any law page (e.g. Wien), verify header search still works, FAQ/Glossar links visible
9. Review screenshots in e2e/screenshots/ for visual quality
  </action>
  <verify>Human approval</verify>
  <done>User approves the visual design of the homepage redesign and all screenshots pass review.</done>
</task>

</tasks>

<verification>
- All new E2E tests pass (hero.spec.js, navigation.spec.js)
- All existing E2E tests pass (card-grid, mobile, search, etc.)
- Full Visual Review Protocol passes per CLAUDE.md
- Screenshots captured for all new UI elements
</verification>

<success_criteria>
- 100% E2E test pass rate across all specs
- New screenshots for: hero-search, hero-search-results, discovery-links, card-grid-collapsed, card-grid-expanded, header-index, header-law-page, mobile-hero, mobile-nav-links
- Updated screenshots for: card-grid-index, mobile-index
- Human approves the visual design of the homepage redesign
</success_criteria>

<output>
After completion, create `.planning/phases/06-search-hero-homepage-navigation/06-03-SUMMARY.md`
</output>
