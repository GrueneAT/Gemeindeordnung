---
phase: 04.1-visual-polish-screenshot-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/assets/gruene-logo.svg
  - scripts/generate-pages.js
  - src/css/main.css
  - e2e/tests/browse-page.spec.js
  - e2e/tests/mobile.spec.js
  - e2e/tests/card-grid.spec.js
  - e2e/tests/typography.spec.js
autonomous: true
requirements: [DSGN-01, DSGN-03, BROW-05, BROW-06]

must_haves:
  truths:
    - "Site header shows the correct Austrian Gruene arrow-G logo, not the sunflower"
    - "All page text uses proper German umlauts where appropriate (not ASCII-safe spellings)"
    - "Typography is readable with proper line-height, max-width, and contrast on desktop and mobile"
    - "No layout issues: no overlapping elements, proper spacing, correct z-index on all pages"
    - "Mobile layout at 375px has no horizontal overflow and adequate touch targets"
    - "All interactive states (tooltips, dropdowns, highlights) are correctly positioned"
  artifacts:
    - path: "src/assets/gruene-logo.svg"
      provides: "Correct Austrian Gruene arrow-G logo SVG"
      contains: "viewBox"
    - path: "e2e/screenshots/browse-page-wien.png"
      provides: "Visual proof of correct header with logo"
    - path: "e2e/screenshots/mobile-index.png"
      provides: "Visual proof of mobile layout quality"
    - path: "e2e/screenshots/typography-law-text.png"
      provides: "Visual proof of readable typography"
  key_links:
    - from: "src/assets/gruene-logo.svg"
      to: "scripts/generate-pages.js"
      via: "img src reference in header template"
      pattern: "gruene-logo\\.svg"
    - from: "scripts/generate-pages.js"
      to: "src/css/main.css"
      via: "TailwindCSS classes in generated HTML"
      pattern: "gruene-"
---

<objective>
Replace the incorrect sunflower logo with the Austrian Gruene arrow-G logo, then systematically review ALL screenshots to find and fix every visual issue (layout, typography, branding, interactive states, mobile). Iterate the build-screenshot-review-fix cycle until all screenshots pass visual inspection.

Purpose: Ship-quality visual polish -- the site should look professional and on-brand before any user sees it.
Output: Clean screenshots proving all visual issues are resolved, correct logo in place.
</objective>

<execution_context>
@/opt/claude-config/get-shit-done/workflows/execute-plan.md
@/opt/claude-config/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-visual-polish-screenshot-review/04.1-CONTEXT.md
@.planning/phases/04.1-visual-polish-screenshot-review/04.1-RESEARCH.md

@scripts/generate-pages.js
@src/css/main.css
@src/assets/gruene-logo.svg
@e2e/playwright.config.js

<interfaces>
<!-- Key references the executor needs -->

From scripts/generate-pages.js (logo usage):
```javascript
const logoPath = isLawPage ? '../assets/gruene-logo.svg' : 'assets/gruene-logo.svg';
// ...
<img src="${logoPath}" alt="Gruene Logo" class="w-8 h-8 gruene-logo" />
```

From src/css/main.css (theme colors):
```css
@theme {
  --color-gruene-green: #6BA539;
  --color-gruene-dark: #005538;
  --color-gruene-light: #E8F5E9;
  --color-gruene-accent: #8AB414;
  --color-gruene-link: #005538;
  --color-gruene-link-hover: #2D5016;
}
```

Build pipeline:
```bash
npm run build && npx pagefind --site dist --force-language de && npx playwright test --config=e2e/playwright.config.js --project=desktop-chromium
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace logo and fix known template issues</name>
  <files>src/assets/gruene-logo.svg, scripts/generate-pages.js</files>
  <action>
1. **Replace the logo SVG** at `src/assets/gruene-logo.svg`:
   - Create a clean SVG of the Austrian Gruene arrow-G logo mark (stylized green "G" incorporating an arrow element, organic brushstroke style)
   - Use the project's `#6BA539` (gruene-green) as the primary fill color
   - Set a square viewBox (e.g., `0 0 32 32` or `0 0 64 64`) so it renders correctly at the `w-8 h-8` (32x32px) container in the header
   - The logo mark only -- no text ("Die Gruenen" / "gruene.at") in the SVG itself; the site name "Gemeindeordnung.at" already appears as text next to it
   - If sourcing from gruene.at or Wikimedia Commons is possible and yields the correct Austrian logo, prefer that over hand-drawing. Otherwise, create an accurate representation.

2. **Fix ASCII-safe spellings in templates** in `scripts/generate-pages.js`:
   - Find all ASCII-safe German text in template strings: "oesterreichischen", "Bundeslaender", "Hauptstueck", "Zurueck", "Gemeinderaetinnen", etc.
   - Replace with proper umlauts: "oesterreichischen" -> "osterreichischen", "Bundeslaender" -> "Bundeslander", "Hauptstueck" -> "Hauptstuck", "Zurueck" -> "Zuruck", etc. (use proper German: oe->o-umlaut, ae->a-umlaut, ue->u-umlaut)
   - All HTML pages have `<meta charset="UTF-8">` so umlauts are safe
   - After changes, run `node scripts/generate-pages.js` to regenerate all HTML

3. **Run the full build-screenshot pipeline:**
   ```bash
   node scripts/generate-pages.js && npm run build && npx pagefind --site dist --force-language de && npx playwright test --config=e2e/playwright.config.js --project=desktop-chromium
   ```

4. **Read and visually review key screenshots** using the Read tool:
   - `e2e/screenshots/browse-page-wien.png` -- verify new logo renders correctly in header at 32x32px
   - `e2e/screenshots/card-grid-index.png` -- verify umlaut text renders correctly
   - `e2e/screenshots/mobile-index.png` -- verify logo and text on mobile

Note: Do NOT commit yet. Task 2 will do the comprehensive review and fix pass.
  </action>
  <verify>
    <automated>npm run build && npx pagefind --site dist --force-language de && npx playwright test --config=e2e/playwright.config.js --project=desktop-chromium</automated>
  </verify>
  <done>
- Logo SVG is the Austrian Gruene arrow-G (not sunflower) and renders cleanly at 32x32px in header
- Template text uses proper German umlauts throughout
- All Playwright tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Systematic screenshot review and fix all visual issues</name>
  <files>src/css/main.css, scripts/generate-pages.js, e2e/tests/browse-page.spec.js, e2e/tests/mobile.spec.js, e2e/tests/card-grid.spec.js, e2e/tests/typography.spec.js</files>
  <action>
**Systematic visual review of ALL 22+ screenshots.** Read each PNG file with the Read tool and check against the full checklist. Fix every issue found, then re-run the pipeline and re-read screenshots to verify fixes.

**Review checklist per screenshot:**
- Layout: no overlapping elements, proper spacing, z-index correct
- Text: umlauts display correctly, no truncation, readable font sizes
- Branding: correct arrow-G logo visible, green colors match Gruene CI
- Typography: proper contrast (WCAG AA), line-height 1.6+, max-width ~70ch for legal text
- Mobile (375px): no horizontal overflow, touch targets adequate, text readable
- Interactive: tooltips positioned, dropdowns layered correctly, search overlay works
- Search UI: dropdown properly layered, results readable, filter chips aligned

**Screenshots to review (read each with Read tool):**

Desktop:
1. `e2e/screenshots/browse-page-wien.png` -- header, footer, overall page layout
2. `e2e/screenshots/card-grid-index.png` -- index page card grid
3. `e2e/screenshots/search-results.png` -- search dropdown with results
4. `e2e/screenshots/search-empty.png` -- search empty state
5. `e2e/screenshots/search-filter-active.png` -- BL filter active
6. `e2e/screenshots/search-filter-all.png` -- all filter state
7. `e2e/screenshots/search-count.png` -- result count display
8. `e2e/screenshots/search-highlight-target.png` -- on-page highlighting
9. `e2e/screenshots/toc-collapsed.png` -- ToC collapsed state
10. `e2e/screenshots/toc-expanded.png` -- ToC expanded state
11. `e2e/screenshots/copy-link-tooltip.png` -- copy link tooltip
12. `e2e/screenshots/scroll-to-top-visible.png` -- scroll-to-top button
13. `e2e/screenshots/scroll-to-top-hidden.png` -- scroll-to-top hidden
14. `e2e/screenshots/anchor-highlight-active.png` -- anchor highlight
15. `e2e/screenshots/anchor-highlight-faded.png` -- anchor highlight faded
16. `e2e/screenshots/dropdown-nav-result.png` -- BL dropdown
17. `e2e/screenshots/typography-law-text.png` -- typography and readability
18. `e2e/screenshots/accessibility-index.png` -- index accessibility
19. `e2e/screenshots/accessibility-law-page.png` -- law page accessibility

Mobile:
20. `e2e/screenshots/mobile-index.png` -- mobile index layout
21. `e2e/screenshots/mobile-law-page.png` -- mobile law page
22. `e2e/screenshots/search-mobile-overlay.png` -- mobile search overlay

**For each issue found:**
1. Identify the source file (CSS in `src/css/main.css` or template in `scripts/generate-pages.js`)
2. Make the fix
3. Regenerate if template changed: `node scripts/generate-pages.js`
4. Re-run: `npm run build && npx pagefind --site dist --force-language de && npx playwright test --config=e2e/playwright.config.js --project=desktop-chromium`
5. Re-read the specific screenshot to verify the fix
6. Repeat until clean

**Add new screenshots if coverage gaps found** (Claude's discretion). Potential gaps identified in research:
- Footer area specifically
- Stadtrechte page (e.g., Innsbruck)
- Deeply nested ToC

If adding new screenshots, add them in the appropriate existing test file or create a focused new spec file.

**Do NOT commit until ALL screenshots pass visual review.** This is the core contract of this phase.
  </action>
  <verify>
    <automated>npm run build && npx pagefind --site dist --force-language de && npx playwright test --config=e2e/playwright.config.js --project=desktop-chromium</automated>
  </verify>
  <done>
- Every screenshot reviewed and confirmed clean (no layout issues, correct branding, readable typography, proper mobile layout)
- All identified issues fixed and verified via re-screenshot
- Any new screenshots added where coverage gaps existed
- All Playwright tests pass
- axe-core accessibility tests pass (WCAG AA)
  </done>
</task>

</tasks>

<verification>
1. All Playwright tests pass: `npx playwright test --config=e2e/playwright.config.js --project=desktop-chromium`
2. Visual verification of all screenshots confirms:
   - Correct Austrian Gruene arrow-G logo in header
   - Proper German umlauts throughout
   - Clean layout on desktop and mobile
   - Readable typography meeting WCAG AA
   - All interactive states working correctly
3. axe-core WCAG AA scan: `npx playwright test --config=e2e/playwright.config.js e2e/tests/accessibility.spec.js`
</verification>

<success_criteria>
- The Austrian Gruene arrow-G logo appears in the header on every page (not the sunflower)
- All template text uses proper German umlauts
- Every screenshot passes visual review with zero issues
- Mobile layout at 375px is clean with no overflow
- WCAG AA accessibility checks pass
- All E2E tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-visual-polish-screenshot-review/04.1-01-SUMMARY.md`
</output>
