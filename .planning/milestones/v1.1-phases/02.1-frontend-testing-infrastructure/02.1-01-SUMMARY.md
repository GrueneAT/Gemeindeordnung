---
phase: 02.1-frontend-testing-infrastructure
plan: 01
subsystem: testing
tags: [playwright, e2e, screenshots, vite-preview, chromium]

# Dependency graph
requires:
  - phase: 02-browsing-branding
    provides: HTML pages with ToC, copy-link buttons, card grid, typography
provides:
  - Playwright E2E testing infrastructure with vite preview webServer
  - 5 UAT E2E test files covering browse, ToC, cards, typography, copy-link
  - Screenshot capture on every test run
  - Desktop and mobile project configurations
affects: [02.1-02, CI pipeline]

# Tech tracking
tech-stack:
  added: ["@playwright/test ^1.58.2", "@axe-core/playwright ^4.11.1"]
  patterns: [E2E tests in e2e/tests/*.spec.js, vite preview as webServer, relative goto paths with baseURL]

key-files:
  created:
    - e2e/playwright.config.js
    - e2e/tests/browse-page.spec.js
    - e2e/tests/toc.spec.js
    - e2e/tests/card-grid.spec.js
    - e2e/tests/typography.spec.js
    - e2e/tests/copy-link.spec.js
  modified:
    - package.json
    - .gitignore

key-decisions:
  - "baseURL includes /src/ path because vite build outputs to dist/src/ due to input files being in src/"
  - "Use ./relative goto paths (not /absolute) so Playwright resolves against baseURL correctly"
  - "NODE_ENV=development required for npm install in this environment (NODE_ENV=production omits devDependencies)"

patterns-established:
  - "E2E test pattern: page.goto('./relative-path') with baseURL http://localhost:4173/gemeindeordnung/src/"
  - "Screenshot pattern: page.screenshot({ path: 'e2e/screenshots/name.png' }) for visual evidence"
  - "ToC details: use '> summary' selector for direct child to avoid nested details ambiguity"

requirements-completed: [TEST-01a, TEST-01b, TEST-01c, TEST-01d, TEST-01e, TEST-02]

# Metrics
duration: 10min
completed: 2026-03-11
---

# Phase 02.1 Plan 01: E2E Testing Infrastructure Summary

**Playwright E2E infrastructure with 5 UAT tests covering browse page, ToC, card grid, typography, and copy-link with screenshot capture**

## Performance

- **Duration:** 10 min
- **Started:** 2026-03-11T05:33:56Z
- **Completed:** 2026-03-11T05:43:34Z
- **Tasks:** 2
- **Files modified:** 8

## Accomplishments
- Playwright installed with chromium browser, configured with vite preview webServer
- 5 E2E tests passing: browse-page, toc, card-grid, typography, copy-link
- Each test captures screenshots to e2e/screenshots/ for visual evidence (TEST-02)
- Desktop (Chrome) and mobile (iPhone 13) project configurations ready

## Task Commits

Each task was committed atomically:

1. **Task 1: Playwright infrastructure setup** - `be472b5` (chore)
2. **Task 2: E2E tests for browse-page, ToC, card-grid, typography, copy-link** - `cecf5e1` (feat)

## Files Created/Modified
- `e2e/playwright.config.js` - Playwright config with webServer, baseURL, two projects
- `e2e/tests/browse-page.spec.js` - UAT 1: header, law content, footer verification
- `e2e/tests/toc.spec.js` - UAT 2: details expand/collapse verification
- `e2e/tests/card-grid.spec.js` - UAT 3: 9 GO + 14 Stadtrecht card verification
- `e2e/tests/typography.spec.js` - UAT 4: line-height >= 1.5 and max-width constraint
- `e2e/tests/copy-link.spec.js` - UAT 5: clipboard write and tooltip verification
- `package.json` - Added test:e2e and test:e2e:ui scripts, playwright deps
- `.gitignore` - Added e2e/screenshots/, e2e/test-results/, playwright-report/

## Decisions Made
- baseURL set to `http://localhost:4173/gemeindeordnung/src/` because vite build outputs HTML under `dist/src/` (input files are in `src/` directory)
- Used `./relative` goto paths instead of `/absolute` so Playwright URL resolution works correctly with baseURL
- Used `> summary` CSS child combinator in ToC test to target direct child summary and avoid nested details ambiguity

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] NODE_ENV=production preventing devDependency installation**
- **Found during:** Task 1 (Playwright infrastructure setup)
- **Issue:** Environment has NODE_ENV=production, causing npm to omit devDependencies silently
- **Fix:** Used `NODE_ENV=development npm install --include=dev` for installation
- **Files modified:** package-lock.json, node_modules/
- **Verification:** `node -e "require.resolve('@playwright/test')"` succeeds
- **Committed in:** be472b5 (Task 1 commit)

**2. [Rule 1 - Bug] Fixed baseURL to include /src/ path segment**
- **Found during:** Task 2 (E2E test creation)
- **Issue:** Plan specified baseURL as `/gemeindeordnung` but vite build outputs to `dist/src/`, so pages are served at `/gemeindeordnung/src/`
- **Fix:** Updated baseURL to `http://localhost:4173/gemeindeordnung/src/` and webServer URL accordingly
- **Files modified:** e2e/playwright.config.js
- **Verification:** All 5 tests pass with correct page navigation
- **Committed in:** cecf5e1 (Task 2 commit)

**3. [Rule 1 - Bug] Fixed ToC test summary selector for nested details**
- **Found during:** Task 2 (E2E test creation)
- **Issue:** `details.first().locator('summary')` matched all 14 nested summaries, causing strict mode violation
- **Fix:** Changed to `details.locator('> summary')` for direct child only
- **Files modified:** e2e/tests/toc.spec.js
- **Verification:** ToC test passes with correct expand/collapse behavior
- **Committed in:** cecf5e1 (Task 2 commit)

---

**Total deviations:** 3 auto-fixed (1 blocking, 2 bugs)
**Impact on plan:** All fixes necessary for correct test execution. No scope creep.

## Issues Encountered
- npm silently ignoring devDependencies due to NODE_ENV=production -- diagnosed by checking `npm config get omit` returning "dev"
- Vite build output structure places HTML under dist/src/ due to input file paths in src/ directory -- required baseURL adjustment

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- E2E infrastructure ready for Plan 02.1-02 (accessibility, responsive, scroll-to-top, dropdown tests)
- All 5 UAT tests passing, screenshot capture working
- Note: `npm run build` must precede `npm run test:e2e` (webServer only serves, doesn't build)

---
*Phase: 02.1-frontend-testing-infrastructure*
*Completed: 2026-03-11*
