---
phase: 04.2-improve-llm-content-quality-searchable-glossary-real-faq-no-placeholder-summaries
plan: 03
type: execute
wave: 2
depends_on:
  - 04.2-01
  - 04.2-02
files_modified:
  - data/llm/summaries/gemeindeordnungen/*.json
  - data/llm/summaries/stadtrechte/*.json
  - data/llm/faq/topics.json
  - data/llm/glossary/terms.json
  - src/faq/*.html
  - src/glossar.html
  - src/gemeindeordnungen/*.html
  - src/stadtrechte/*.html
autonomous: false
requirements:
  - LLM-01
  - LLM-02
  - LLM-03
  - LLM-04
  - LLM-05
  - LLM-06
  - LLM-07
  - DEPL-03

must_haves:
  truths:
    - "All 23 summary files have placeholder:false or no placeholder flag"
    - "Summaries use varied natural language, not formulaic openers"
    - "FAQ topics are substantive with real BL-specific details"
    - "FAQ references use proper legal citation format"
    - "Glossary terms have definitions with proper umlauts"
    - "Glossary references span multiple Bundeslaender"
    - "Topic tags are specific and granular, not just 'Allgemeine Bestimmungen'"
    - "npm run llm:validate exits with code 0"
  artifacts:
    - path: "data/llm/summaries/gemeindeordnungen/burgenland.json"
      provides: "Real LLM-generated summaries for Burgenland"
    - path: "data/llm/faq/topics.json"
      provides: "Real FAQ content with substantive answers"
    - path: "data/llm/glossary/terms.json"
      provides: "Real glossary with cross-BL references"
  key_links:
    - from: "data/llm/summaries/*/*.json"
      to: "src/gemeindeordnungen/*.html"
      via: "generate-pages.js reads JSON and renders HTML"
      pattern: "llmData"
    - from: "data/llm/faq/topics.json"
      to: "src/faq/*.html"
      via: "generate-pages.js reads topics and generates FAQ pages"
      pattern: "generateFAQPages"
    - from: "data/llm/glossary/terms.json"
      to: "src/glossar.html"
      via: "generate-pages.js reads terms and generates glossary page"
      pattern: "generateGlossaryPage"
---

<objective>
Regenerate all LLM content using the improved pipeline and verify quality.

Purpose: Replace all placeholder content with high-quality, varied, natural language summaries, substantive FAQ, and comprehensive glossary -- the actual content users will read.
Output: All 23 summary files, FAQ topics.json, glossary terms.json regenerated with real content; all pages rebuilt; validation passes.
</objective>

<execution_context>
@/opt/claude-config/get-shit-done/workflows/execute-plan.md
@/opt/claude-config/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.2-improve-llm-content-quality-searchable-glossary-real-faq-no-placeholder-summaries/04.2-CONTEXT.md
@.planning/phases/04.2-improve-llm-content-quality-searchable-glossary-real-faq-no-placeholder-summaries/04.2-01-SUMMARY.md
@.planning/phases/04.2-improve-llm-content-quality-searchable-glossary-real-faq-no-placeholder-summaries/04.2-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate all LLM content and rebuild site</name>
  <files>data/llm/summaries/gemeindeordnungen/*.json, data/llm/summaries/stadtrechte/*.json, data/llm/faq/topics.json, data/llm/glossary/terms.json</files>
  <action>
IMPORTANT: The LLM pipeline uses `claude -p` CLI which CANNOT run inside a Claude Code session (nested Claude invocation). The executor must handle this:

1. First attempt: Try running `npm run llm:all` directly. If it works (claude CLI available and not nested), proceed.

2. If claude CLI fails (nested session detection): Create a checkpoint-like instruction for the user:
   - Print clear message: "Please run 'npm run llm:all' outside this Claude Code session, then resume."
   - Wait for user confirmation that it completed.

3. After LLM content is generated (by whichever path), run:
   ```bash
   npm run llm:validate
   ```
   Verify exit code 0. If validation fails, inspect errors and decide:
   - If minor issues (a few short summaries), note them but proceed
   - If major issues (placeholder flag still present, widespread ASCII-safe spellings), the regeneration failed

4. Rebuild the site with regenerated content:
   ```bash
   node scripts/generate-pages.js
   npm run build
   npx pagefind --site dist --force-language de
   ```

5. Run the full E2E test suite to verify rendered content:
   ```bash
   npx playwright test --config=e2e/playwright.config.js --project=desktop-chromium
   ```

6. If any E2E tests fail due to changed content (e.g., test expects specific placeholder text), update the test expectations to match the new real content patterns.
  </action>
  <verify>
    <automated>npm run llm:validate && npm test && npm run build && npx pagefind --site dist --force-language de && npx playwright test --config=e2e/playwright.config.js --project=desktop-chromium</automated>
  </verify>
  <done>All 23 summary files regenerated without placeholder flag, FAQ has real substantive content, glossary has cross-BL references, validation passes, all tests pass, site builds successfully</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of regenerated LLM content quality</name>
  <files>src/gemeindeordnungen/*.html, src/faq/*.html, src/glossar.html</files>
  <action>
Present the regenerated content to the user for quality verification. Start the preview server with `npm run preview:search` and guide the user through the verification steps below.
  </action>
  <verify>User confirms content quality is acceptable</verify>
  <done>User types "approved" confirming summaries are natural and varied, FAQ has substantive answers with proper citations, glossary has comprehensive definitions with cross-BL references, and glossary filter works</done>
  <what-built>Complete LLM content regeneration: all summaries, FAQ, and glossary replaced with high-quality natural language content. Glossary page now has client-side search filter.</what-built>
  <how-to-verify>
    1. Run `npm run preview:search` to start local preview server
    2. Open http://localhost:4173 (or whatever port Vite reports)

    **Summaries (pick 3 random BL):**
    3. Navigate to a Gemeindeordnung page (e.g., Burgenland)
    4. Expand a few "Vereinfachte Zusammenfassung" sections
    5. Verify: summaries are natural language, varied openers (not "Dieser Paragraph regelt..." every time)
    6. Verify: proper German umlauts (not ASCII-safe oe/ae/ue)
    7. Verify: topic filter chips show specific labels (not all "Allgemeine Bestimmungen")

    **FAQ:**
    8. Click "FAQ" in the header navigation
    9. Browse 2-3 topic pages
    10. Verify: questions have real substantive answers with BL-specific details
    11. Verify: references use proper citation format (e.g., "Par. 15 Bgld. GO" not "Par. 15 burgenland")
    12. Verify: answers are well-structured (answer first, then BL details)

    **Glossary:**
    13. Click "Glossar" in the header navigation
    14. Verify: terms have proper definitions with umlauts
    15. Verify: references span multiple Bundeslaender (not just first 3)
    16. Type in the new filter input -- verify terms filter instantly
    17. Clear filter -- verify all terms reappear

    **General:**
    18. Check that no page shows obvious LLM artifacts (markdown formatting, JSON fragments, etc.)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm run llm:validate` exits with code 0
- `npm test` passes (unit tests)
- `npx playwright test --config=e2e/playwright.config.js` passes (E2E)
- Human verification confirms content quality
</verification>

<success_criteria>
- Zero placeholder flags in any LLM JSON file
- Summaries use varied, natural German with proper umlauts
- FAQ has substantive answers with BL-specific details and proper citations
- Glossary has comprehensive definitions with cross-BL references
- Topic tags are specific and granular
- All automated tests pass
- Human approves content quality
</success_criteria>

<output>
After completion, create `.planning/phases/04.2-improve-llm-content-quality-searchable-glossary-real-faq-no-placeholder-summaries/04.2-03-SUMMARY.md`
</output>
