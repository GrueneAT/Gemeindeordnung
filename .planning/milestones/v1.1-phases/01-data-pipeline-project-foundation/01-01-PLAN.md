---
phase: 01-data-pipeline-project-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.js
  - src/index.html
  - src/css/main.css
  - src/js/main.js
  - scripts/config.js
  - scripts/fetch-laws.js
  - tests/fetch-laws.test.js
  - .gitignore
autonomous: true
requirements:
  - DATA-01
  - DATA-03
  - DSGN-02

must_haves:
  truths:
    - "Running `node scripts/fetch-laws.js` downloads all 23 laws (9 GO + 14 Stadtrechte) from RIS as HTML files"
    - "Each fetched HTML file is 100KB+ (real law content, not an error page)"
    - "Fetch script aborts with error if any single law fails to download"
    - "Vite dev server starts and serves a page styled with TailwindCSS"
  artifacts:
    - path: "scripts/config.js"
      provides: "Complete registry of all 23 laws with GeltendeFassung URLs"
      exports: ["LAWS"]
    - path: "scripts/fetch-laws.js"
      provides: "Fetches all laws from RIS, saves to data/raw/"
      min_lines: 40
    - path: "vite.config.js"
      provides: "Vite build config with TailwindCSS v4 plugin"
    - path: "src/css/main.css"
      provides: "TailwindCSS v4 entry with Gruene theme colors"
      contains: "@import \"tailwindcss\""
  key_links:
    - from: "scripts/fetch-laws.js"
      to: "scripts/config.js"
      via: "import LAWS"
      pattern: "import.*LAWS.*from.*config"
    - from: "scripts/fetch-laws.js"
      to: "data/raw/"
      via: "fs.writeFileSync"
      pattern: "writeFile"
---

<objective>
Set up the project from scratch and implement the RIS law fetching pipeline.

Purpose: Establish the foundation (Node.js project, Vite + TailwindCSS v4, test framework) and build the first pipeline stage -- fetching all 23 Austrian laws (9 Gemeindeordnungen + 14 Statutarstadt-Stadtrechte) from RIS GeltendeFassung URLs.

Output: Working project with `npm run dev`, complete law registry in config.js, and fetch script that downloads all laws.
</objective>

<execution_context>
@/opt/claude-config/get-shit-done/workflows/execute-plan.md
@/opt/claude-config/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-pipeline-project-foundation/01-CONTEXT.md
@.planning/phases/01-data-pipeline-project-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize project with Vite, TailwindCSS v4, and vitest</name>
  <files>package.json, vite.config.js, src/index.html, src/css/main.css, src/js/main.js, .gitignore</files>
  <action>
1. Run `npm init -y` at project root. Set `"type": "module"` in package.json.
2. Install dev dependencies: `npm install -D vite @tailwindcss/vite tailwindcss cheerio vitest`
3. Create `vite.config.js` with TailwindCSS v4 plugin (`@tailwindcss/vite`). Set `base` to `'/gemeindeordnung/'` (repo name placeholder -- can be updated later). Use rollupOptions.input to specify `src/index.html`.
4. Create `src/index.html` -- minimal landing page with German text: "Gemeindeordnungen der oesterreichischen Bundeslaender". Include `<link>` to `css/main.css` and `<script>` for `js/main.js`. Add a `<div id="app">` with placeholder content.
5. Create `src/css/main.css` with TailwindCSS v4 entry point:
   - `@import "tailwindcss";`
   - `@theme` block with Gruene colors: `--color-gruene-green: #6BA539;`, `--color-gruene-dark: #005538;`, `--color-gruene-light: #E8F5E9;`
6. Create `src/js/main.js` -- minimal JS (can be empty or console.log for now).
7. Create `.gitignore` with: `node_modules/`, `dist/`, `data/raw/` (raw HTML is fetched, not committed).
8. Add npm scripts to package.json: `"dev": "vite"`, `"build": "vite build"`, `"preview": "vite preview"`, `"test": "vitest run"`, `"fetch": "node scripts/fetch-laws.js"`.
9. Create directory structure: `scripts/`, `data/raw/`, `data/parsed/gemeindeordnungen/`, `data/parsed/stadtrechte/`, `tests/fixtures/`.

IMPORTANT TailwindCSS v4 notes (do NOT use v3 patterns):
- NO `tailwind.config.js` -- use `@theme` in CSS
- NO `postcss.config.js` -- use `@tailwindcss/vite` plugin
- NO `@tailwind base/components/utilities` -- use `@import "tailwindcss"`
  </action>
  <verify>
    <automated>cd /root/workspace && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>Vite builds successfully with TailwindCSS v4. `npm run dev` starts dev server. Project structure matches research recommendations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create law registry config and fetch script</name>
  <files>scripts/config.js, scripts/fetch-laws.js, tests/fetch-laws.test.js</files>
  <behavior>
    - Test 1: config.js exports LAWS object with gemeindeordnungen (9 entries) and stadtrechte (14 entries), each with name, abfrage, gesetzesnummer, url, category
    - Test 2: All 23 URLs start with "https://www.ris.bka.gv.at/GeltendeFassung.wxe"
    - Test 3: No duplicate gesetzesnummer+abfrage combinations (composite key uniqueness)
    - Test 4: fetch-laws.js fetchLaw() throws on HTTP error (mocked)
    - Test 5: fetch-laws.js fetchLaw() throws if response body < 10000 bytes (error page detection)
  </behavior>
  <action>
1. Create `scripts/config.js` exporting `LAWS` object with two categories:
   - `gemeindeordnungen`: All 9 Bundeslaender (Burgenland, Kaernten, Niederoesterreich, Oberoesterreich, Salzburg, Steiermark, Tirol, Vorarlberg, Wien). Wien uses Wiener Stadtverfassung (Gesetzesnummer 20000308, Abfrage LrW).
   - `stadtrechte`: All 14 Statutarstaedte (Eisenstadt, Rust, Klagenfurt, Villach, Krems, St. Poelten, Waidhofen/Ybbs, Wr. Neustadt, Linz, Steyr, Wels, Salzburg Stadt, Graz, Innsbruck).
   Each entry has: `name` (official Kurztitel), `abfrage`, `gesetzesnummer`, `url` (full GeltendeFassung URL), `category` ("gemeindeordnung" or "stadtrecht"), `stadt` (null for GO, city name for Stadtrechte), `bundesland`.
   Use exact values from RESEARCH.md RIS Law Registry tables.

2. Create `scripts/fetch-laws.js`:
   - Import LAWS from config.js
   - `fetchLaw(key, config)`: fetch URL, validate HTTP status, validate response size >= 10000 bytes (error page detection), return HTML string
   - `fetchAll()`: iterate all laws in both categories, call fetchLaw with 1.5s delay between requests (rate limiting), save each to `data/raw/{category}/{key}.html`, log progress
   - On ANY fetch failure: throw error immediately (fail-fast per user decision)
   - Export `fetchLaw` for testing
   - When run directly (`import.meta.url`): call fetchAll()

3. Create `tests/fetch-laws.test.js`:
   - Test config completeness (9 GO + 14 Stadtrechte = 23 total)
   - Test URL format validation
   - Test composite key uniqueness
   - Test fetchLaw error handling with mocked fetch (use vitest mocking)

NOTE: Do NOT run the actual fetch script against RIS in this task -- that happens during CI or manual runs.
  </action>
  <verify>
    <automated>cd /root/workspace && npx vitest run tests/fetch-laws.test.js</automated>
  </verify>
  <done>Config contains all 23 laws with correct URLs. Fetch script handles errors correctly. All tests pass.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors (Vite + TailwindCSS v4)
- `npx vitest run` passes all fetch-related tests
- `scripts/config.js` contains exactly 23 law entries (9 + 14)
- `.gitignore` excludes `node_modules/`, `dist/`, `data/raw/`
</verification>

<success_criteria>
- Project initializes and builds with Vite + TailwindCSS v4 (no v3 patterns)
- Complete law registry with all 23 verified GeltendeFassung URLs
- Fetch script with rate limiting, size validation, and fail-fast error handling
- Tests validate config completeness and fetch error handling
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-pipeline-project-foundation/01-01-SUMMARY.md`
</output>
